name: settlement_qa_agent
description: Evidence-bound settlement tracing dashboard.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  intl: ^0.20.2
  google_fonts: ^6.3.1
  flutter_svg: ^2.2.0
  fl_chart: ^0.70.2
  shared_preferences: ^2.5.3
  shimmer: ^3.0.0
  share_plus: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
